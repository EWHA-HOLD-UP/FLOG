//floging screen
import 'package:flutter/material.dart';

class Mainscreen extends StatelessWidget {
  const Mainscreen({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Floging Screen - 하단 탭 구현 위해서 임의로 생성'),
    );
  }
}