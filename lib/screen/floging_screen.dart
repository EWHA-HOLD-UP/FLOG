//floging screen
import 'package:flutter/material.dart';

class Floging_Screen extends StatelessWidget {
  const Floging_Screen({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('floging screen'),
      ),
    );
  }
}