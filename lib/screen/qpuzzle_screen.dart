import 'package:flutter/material.dart';

class Qpuzzle_screen extends StatelessWidget {
  const Qpuzzle_screen({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('q-puzzle screen'),
      ),
    );
  }
}